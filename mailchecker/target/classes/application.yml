logging:
  level:
    org:
      springframework:
        security: debug
server:
  port: 8081
spring:
  application:
    name: mailchecker
  datasource:
    password: password
    url: jdbc:postgresql://143.47.182.172:5432/mailchecker
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true